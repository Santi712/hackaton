
// import { Link } from 'react-router-dom';

export default function Home() {


    return(
        <>
            {/* {courseData && courseData.map((course,index)=>{
                return(
                    <Link to={`/curso/:id}`} key={index}>
                        <p>{course.title}</p>
                    </Link>
                    
                )
            })} */}
            
        </>
    )
}

